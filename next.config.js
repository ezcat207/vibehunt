/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'vercel.app',
      'lovable.app',
      'base44.app',
      'youware.app',
      'www.google.com' // Favicon API
    ],
  }
};

module.exports = nextConfig;
